---
  - name: connectivity for server
    hosts: db
    become_user: admum
    become: yes
    tasks:
       - name: install packages on system as domain user
         yum:
           name: telnet
           state: latest

       - name: install netcat
         yum: name=nc state=latest
