---
-   name: testing connectivity
    hosts: infr
    become: yes
    become_user: root
    gather_facts: no
    tasks:
      - name: package install for servers
        yum:  name=ncdu state=latest
